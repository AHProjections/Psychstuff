services:
  - type: web
    name: psychstuff
    runtime: node
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: TWITTER_BEARER_TOKEN
        sync: false   # set this manually in Render dashboard for live X posts
